// const { createClient } = require("redis");
// const client = createClient(2525);
// client.on("error", (err) => console.log("Redis Client Error", err));
// client.connect();
// module.exports = client;
